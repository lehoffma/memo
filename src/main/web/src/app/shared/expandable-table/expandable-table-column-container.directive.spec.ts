import {ExpandableTableColumnContainerDirective} from "./expandable-table-column-container.directive";

describe("ExpandableTableColumnContainerDirective", () => {
	it("should create an instance", () => {
		const directive = new ExpandableTableColumnContainerDirective();
		expect(directive).toBeTruthy();
	});
});
