import { DisplayErrorDirective } from './display-error.directive';

describe('DisplayErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayErrorDirective();
    expect(directive).toBeTruthy();
  });
});
