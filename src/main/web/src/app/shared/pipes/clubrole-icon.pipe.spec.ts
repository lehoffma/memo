import { ClubRoleIconPipe } from './club-role-icon.pipe';

describe('ClubRoleIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ClubRoleIconPipe();
    expect(pipe).toBeTruthy();
  });
});
