import {SpiedOnElementDirective} from "./spied-on-element.directive";

describe("SpiedOnElementDirective", () => {
	it("should create an instance", () => {
		const directive = new SpiedOnElementDirective();
		expect(directive).toBeTruthy();
	});
});
