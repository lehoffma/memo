import { UserPreviewDirective } from './user-preview.directive';

describe('UserPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new UserPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
